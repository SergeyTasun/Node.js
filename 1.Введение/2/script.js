/*
Используя инструмент REPL, определите функцию mul(), которая принимает два параметра и возвращает их произведение.
Запустите функцию mul() несколько раз с разными значениями.
*/

function mul(x, y) {
   return x * y;
}

console.log(mul(2, 2));
console.log(mul(6, 2));
console.log(mul(1.7, 1.5));